name: Deploy to GitHub Pages
   
   on:
     push:
       branches: [ main ]
   
   jobs:
     deploy:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v3
         - name: Deploy to GitHub Pages
           uses: peaceiris/actions-gh-pages@v3
           with:
             github_token: ${{ secrets.GITHUB_TOKEN }}
             publish_dir: ./
```

4. Cliquez **"Commit changes"**

5. Votre site sera automatiquement déployé !

---

## 💡 **SOLUTION LA PLUS SIMPLE : Netlify Drop**

Si GitHub est compliqué, essayez **Netlify** (BEAUCOUP plus simple) :

1. **Allez sur** : https://app.netlify.com/drop

2. **Glissez-déposez** votre fichier `recouvrement-mobile.html` directement sur la page

3. **C'est tout !** Netlify vous donne une URL instantanément :
```
   https://random-name-123456.netlify.app
